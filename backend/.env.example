# Server Configuration
PORT=3001

# Data Source Configuration ('json' or 'typeorm')
DATA_SOURCE=json

# Database Configuration (only used when DATA_SOURCE=typeorm)
DB_TYPE=sqlite
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=
DB_PASSWORD=
DB_DATABASE=database.sqlite
DB_SYNCHRONIZE=true
DB_LOGGING=false

# JSON Database Configuration (only used when DATA_SOURCE=json)
JSON_DB_PATH=./db.json